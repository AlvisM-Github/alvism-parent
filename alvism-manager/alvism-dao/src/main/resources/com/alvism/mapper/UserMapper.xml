<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.alvism.mapper.UserMapper">

    <select id="getUserById" parameterType="string" resultType="com.alvism.pojo.User">
        SELECT * FROM base_user WHERE id = #{value}
    </select>

</mapper>